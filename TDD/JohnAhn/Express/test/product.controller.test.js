// import { createProduct } from "../routers/product/product.controller.js";

describe("Product Controller Create", () => {
  it("should have a createProduct funtcion", () => {
    // expect(typeof createProduct).toBe("function");
  });
});
